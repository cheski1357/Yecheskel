// 3 - Write a function called onlyIf that takes 3 parameters, an array, a 
// 'test' and an 'action'.Call the action for each element of the array that
//  passes the test - but not
//  for those that don't - kind of like a combination of filter and forEach. 

'use strict'

 function onlyIf(theArray, test, action){
      forEach(element => {
          if(test(element)){
              action(element);

          }
         
     });{

        if(test(theArray[i]))


     }
 }
 onlyIf